{
  "name": "utm-builder",
  "private": true,
  "description": "Dev-only test harness for the UTM Builder. The app itself is a dependency-free static site — nothing here ships to production.",
  "type": "module",
  "scripts": {
    "test": "node tests/e2e.mjs"
  },
  "devDependencies": {
    "playwright": "^1.56.1"
  }
}
